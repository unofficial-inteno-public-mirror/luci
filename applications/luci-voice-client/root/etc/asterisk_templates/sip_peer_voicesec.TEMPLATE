[|NAME|]
type		= peer
defaultuser	= |AUTHUSER|	; Authentication user for outbound proxies
fromuser	= |USER|	; How your provider knows you
#exec echo "secret = $(/usr/bin/voicesec -d $(cat /usr/lib/asterisk/voicesec_|NAME|))"
host		= |HOST|
fromdomain	= |FROMDOMAIN|
insecure	= port,invite
canreinvite	= no
insecure	= invite
;qualify		= yes
nat		= yes
context		= incoming_calls
port=|PORT|
outboundproxy=|OUTBOUNDPROXY||OUTBOUNDPROXYPORT|
disallow	= all
|ALLOW|

